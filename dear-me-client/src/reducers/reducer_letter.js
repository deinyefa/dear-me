export default function(state = {}, action) {
	switch (action.type) {
		case 'SEND_LETTER':
			return
        (...state;
          console.log(action.payload);
        )

		default:
			return state;
	}
}
